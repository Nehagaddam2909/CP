#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ll n;
    cin>>n;
    while(n>0)
    {
        
        ll a,b,t;
        string s;
        set<string>set_s;
        cin>>a>>s;
        for(ll i=0;i<a;i++)
            s.insert(s[])
        
        n--;
    }
    return 0;
}